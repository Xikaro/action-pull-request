name: GitHub Action for creating Pull Requests
author: Krzysztof Szyper / ChristophShyper / shyper.pro
description: GitHub Action that will create a pull request from the current branch
inputs:
  github_token:
    description: GitHub token
    required: true
  repository:
    description: Repository in owner/name format used for API calls and git remote auth (defaults to current repository)
    required: false
    default: ""
  repository_path:
    description: Relative path under GITHUB_WORKSPACE to the checked-out repository (use when actions/checkout path is set)
    required: false
    default: "."
  source_branch:
    description: Name of the source branch
    required: false
    default: ""
  target_branch:
    description: Name of the target branch
    required: false
    default: master
  title:
    description: Pull request title
    required: false
    default: ""
  template:
    description: Template file location
    required: false
    default: ""
  body:
    description: Pull request body
    required: false
    default: ""
  reviewer:
    description: Reviewer's username
    required: false
    default: ""
  assignee:
    description: Assignee's usernames
    required: false
    default: ""
  label:
    description: Labels to apply, coma separated
    required: false
    default: ""
  create_missing_labels:
    description: Whether to create labels that do not already exist before creating the PR
    required: false
    default: "false"
  milestone:
    description: Milestone
    required: false
    default: ""
  project:
    description: GitHub Project title to add the pull request to
    required: false
    default: ""
  draft:
    description: Whether to mark it as a draft
    required: false
    default: "false"
  old_string:
    description: Old string for the replacement in template
    required: false
    default: ""
  new_string:
    description: New string for the replacement in the template
    required: false
    default: ""
  get_diff:
    description: Whether to inject difference into template
    required: false
    default: "false"
  ignore_users:
    description: List of users to ignore, coma separated
    required: false
    default: dependabot
  allow_no_diff:
    description: Allows to continue on merge commits with no diffs
    required: false
    default: "false"
  max_body_bytes:
    description: Maximum PR body size in bytes before overflow is moved into managed comments
    required: false
    default: "65000"
  max_diff_lines:
    description: Maximum lines per generated diff section (0 means unlimited)
    required: false
    default: "0"
  auto_merge:
    description: Whether to automatically merge the pull request after creation or update
    required: false
    default: "false"
  auto_merge_method:
    description: Merge method to use when auto_merge is enabled (merge, squash, or rebase)
    required: false
    default: squash
  auto_merge_require_approval:
    description: Whether to require at least one approval before auto-merging
    required: false
    default: "true"
  auto_merge_wait_checks:
    description: Whether to wait for all required status checks to pass before auto-merging
    required: false
    default: "true"
outputs:
  url:
    description: Pull request URL
  pr_number:
    description: Pull request number
runs:
  using: docker
  image: docker://devopsinfra/action-pull-request:v1.4.0
  env:
    GITHUB_TOKEN: ${{ inputs.github_token }}
branding:
  color: purple
  icon: upload-cloud
